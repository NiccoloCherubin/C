// INCREMENTO UNITARIO
#include <stdio.h>
void main()
{
    int contatore, var1;       // Per incrementare. Sempre tra gli interi
    contatore = 0;             // sempre inizializzare a 0 (non sempre)
    contatore = contatore + 1; // contatore++
    printf("Contatore = %d", contatore);
}